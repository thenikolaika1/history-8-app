(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const nav=$('.quick'),search=$('#searchInput'),noResults=$('#noResults');let searchTimer;
function cards(){return $$('.paragraph')}
function setActive(id){if(!nav)return;$$('a',nav).forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+id));const a=$(`a[href="#${id}"]`,nav);if(a)a.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'})}
function activeOnScroll(){const visible=cards().filter(c=>!c.classList.contains('hidden'));if(!visible.length)return;const line=window.innerHeight*.32;let best=visible[0],d=Infinity;visible.forEach(c=>{const r=c.getBoundingClientRect(),x=Math.abs(r.top-line);if(r.bottom>90&&x<d){d=x;best=c}});setActive(best.id)}
if(nav){nav.addEventListener('click',e=>{const a=e.target.closest('a[href^="#"]');if(!a)return;const id=a.getAttribute('href').slice(1),card=document.getElementById(id);if(!card)return;e.preventDefault();card.scrollIntoView({behavior:'smooth',block:'start'});setActive(id);card.classList.remove('jump-flash');void card.offsetWidth;card.classList.add('jump-flash');setTimeout(()=>card.classList.remove('jump-flash'),900)})}
let ticking=false;addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(()=>{activeOnScroll();ticking=false});ticking=true}},{passive:true});
if(search){search.addEventListener('input',()=>{const label=search.closest('.search');label?.classList.add('searching');clearTimeout(searchTimer);searchTimer=setTimeout(()=>{label?.classList.remove('searching');cards().filter(c=>!c.classList.contains('hidden')).forEach(c=>{c.classList.remove('result-in');void c.offsetWidth;c.classList.add('result-in')});if(noResults){noResults.classList.toggle('show',getComputedStyle(noResults).display!=='none')}activeOnScroll()},170)})}
function collapsibles(){$$('.terms,.people,.world-dates').forEach(box=>{if(box.dataset.collapseReady)return;const h=$('h3',box);if(!h)return;box.dataset.collapseReady='1';h.setAttribute('role','button');h.setAttribute('tabindex','0');h.setAttribute('aria-expanded','true');const toggle=()=>{const closed=box.classList.toggle('collapsed');h.setAttribute('aria-expanded',String(!closed))};h.addEventListener('click',toggle);h.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle()}})})}
const mo=new MutationObserver(()=>{collapsibles();activeOnScroll()});const root=$('#paragraphs');if(root)mo.observe(root,{childList:true,subtree:true});collapsibles();
const top=document.createElement('button');top.id='toTop';top.type='button';top.setAttribute('aria-label','Наверх');top.textContent='↑';document.body.appendChild(top);top.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));function topState(){top.classList.toggle('show',scrollY>700)}addEventListener('scroll',topState,{passive:true});topState();activeOnScroll()})();